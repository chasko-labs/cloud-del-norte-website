name: Deploy awsaerospace-core to S3

on:
  push:
    branches: [main]
    paths:
      - 'awsaerospace-core/**'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: awsaerospace-core/package-lock.json
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        role-session-name: GitHubActions-Core-Deploy
        aws-region: ${{ env.AWS_REGION }}
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      continue-on-error: true
      id: aws-config
    
    - name: Install dependencies
      working-directory: awsaerospace-core
      run: npm install
    
    - name: Build
      working-directory: awsaerospace-core
      run: npm run build
    
    - name: Deploy to S3
      if: steps.aws-config.outcome == 'success'
      working-directory: awsaerospace-core
      run: |
        aws s3 sync lib/ s3://awsaerospace.org/ --delete --exact-timestamps
    
    - name: Invalidate CloudFront
      if: steps.aws-config.outcome == 'success'
      run: |
        aws cloudfront create-invalidation --distribution-id ECC3LP1BL2CZS --paths "/*"
